<h1 style="text-align: center">Comedians</h1>



<div class="container">

    <% @users.each do |user| %>
        <div class="tile">
        <p class="profile-pic"> <%= link_to(image_tag(user.pic), user_path(user)) %></p><br>
        <p><%= link_to user.name, user_path(user) %></p>
        
        </div>
    <% end %>
 </div>



 